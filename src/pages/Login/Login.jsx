import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Login = () => {
    useTitle("Login");
    return (
        <div>
            <h1>Login Page</h1>
        </div>
    );
};

export default Login;